import Foundation

struct AdditionalServicesPresenter {
    
    init(scene: HybridWebScene) {
        scene.setSceneTitle(.additionalServices)
        scene.setSceneShortTitle(.services)
    }
    
}
